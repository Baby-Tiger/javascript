/* We can use events to call the function.
Basic events .......

Click(onclick),
Double Click (ondblclick),
Right Click (oncontextmenu),
Mouse Hover (onmouseenter),
Mouse Out (onmouseout),
Mouse Down (onmousedown),
Key Press (onkeypress) only works in body tag or foams,
Key Up (onkeyup),
Load (onload) only works in body tag ,
Unload (onunload) only works in body tag,
Resize (onresize) only works in body tag,
Scroll (onscroll) 


// Declaring Function

function hello() {
  document.write("Hello Everyone");
}

// Calling function in html button tag


